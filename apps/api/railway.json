{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'echo --- Starting migrations --- && (alembic upgrade head || echo WARNING: migrations failed, continuing...) && echo --- Starting seed --- && (python seed_data.py || echo WARNING: seed failed, continuing...) && echo --- Starting uvicorn --- && uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
